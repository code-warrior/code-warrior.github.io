/*jslint browser, es6, single, for, devel, this */
/*global window */

let message = document.getElementById('msg');

message.innerHTML = 'The method <code>getElementById</code> establishes a reference to a DOM element whose ID attribute matches <code>getElementById</code>’s parameter. For example, <code>getElementById(\'msg\')</code> references the paragraph on this page whose ID is <code>msg</code>. This content is written into that paragraph via <code>getElementById</code>.';
